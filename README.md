# Cortrix Website

Static website source for Cortrix.

## Files

- `index.html` contains the page markup.
- `styles.css` contains the page styling.
- `script.js` contains the page behavior.

## Local preview

Open `index.html` in a browser, or serve this directory with any static file server.
